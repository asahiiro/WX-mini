<view class="sky">
  <view
    wx:for="{{clouds}}"
    wx:key="id"
    class="dynamic-cloud"
    style="width: {{item.size}}rpx; height: {{item.size}}rpx; left: ({{item.left}}*10)rpx; top: （{{item.top}}*10）rpx; animation-delay: {{item.animationDelay}}s; z-index: 0;"
  ></view>
</view>