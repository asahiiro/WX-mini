<view class="sky">
  <view
    wx:for="{{clouds}}"
    wx:key="id"
    class="dynamic-cloud"
    style="width: {{item.size}}rpx; height: {{item.size}}rpx; left: {{item.left}}rpx; top: {{item.top}}rpx; animation-delay: {{item.animationDelay}}s; z-index: {{item.zIndex}};"
  ></view>
</view>